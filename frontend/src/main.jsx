import React from 'react'
import ReactDOM from 'react-dom/client' //Broswer 
import App from './App.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render( // To create the app 
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
